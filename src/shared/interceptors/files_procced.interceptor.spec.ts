import { FilesProccedInterceptor } from './files_procced.interceptor';

describe('FilesProccedInterceptor', () => {
  it('should be defined', () => {
    expect(new FilesProccedInterceptor()).toBeDefined();
  });
});
